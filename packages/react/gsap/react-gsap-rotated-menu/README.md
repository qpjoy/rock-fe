# CRA gsap menu

```bash
yarn create react-app react-gsap-rotated-menu --template typescript

yarn add gsap styled-components styled-reset

yarn add @babel/plugin-proposal-private-property-in-object -D
```
