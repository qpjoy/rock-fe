# scripts

```bash
yarn create react-app ulti-react --template typescript
yarn add @craco/craco @craco/types -D
# add craco.config.js && scripts in package.json
yarn add -D @babel/plugin-proposal-private-property-in-object
yarn add sass -D
```